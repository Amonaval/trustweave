import {parseArgs} from '../agentic/lib.mjs';import {runRiskBoard} from './risk-board.mjs';const a=parseArgs();if(!a.mission||!a.candidate||!a.out)throw new Error('--mission, --candidate and --out are required');const result=runRiskBoard({mission:a.mission,candidate:a.candidate,evidencePath:a.evidence||null,out:a.out,reviewer:a.reviewer||'risk-board/process-v1',builder:a.builder||'work-session/root'});console.log(JSON.stringify(result,null,2));if(result.status!=='APPROVED')process.exit(1);
