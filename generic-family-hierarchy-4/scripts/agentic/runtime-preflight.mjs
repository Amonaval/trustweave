import {parseArgs,readJson,printChecks,resolveMissionPath} from './lib.mjs';const a=parseArgs();const mission=resolveMissionPath(a.mission);const m=readJson(mission),q=readJson('governance/quality-policy.json'),c=[];const ok=(n,v,d='')=>c.push([n,!!v,d]);for(const g of m.gates){const def=q.gateRegistry[g];if(def?.kind!=='runtime')continue;const missing=(def.requiredEnv||[]).filter(k=>!process.env[k]);ok(`runtime preconditions: ${g}`,missing.length===0,missing.length?`missing ${missing.join(', ')}`:'ready')}printChecks('Runtime preflight',c);
