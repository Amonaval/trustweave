# M3-C3 Evidence

The environment manager restores locked dependencies, reports preconditions without values, starts and waits for the real Next preview, launches a real Chromium binary supplied as a locked development dependency, captures page evidence, and terminates the preview. The unavailable Playwright CDN was classified as an environment defect and repaired without Founder relay by selecting the locked package route.

Founder interventions: 0. Manual error relays: 0.
