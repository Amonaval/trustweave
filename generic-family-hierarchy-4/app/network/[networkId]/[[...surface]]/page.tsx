import NetworkApp from "../../../../components/NetworkApp";

// The shell resolves membership before rendering a network. URLs alone confer no access.
export default function NetworkRoutePage(){return <NetworkApp/>}
